import { Box } from "@mui/material";
import React from "react";

const SideBar: React.FC = () => {
  return (
    <Box sx={{ backgroundColor: "yellow", flex: "5%", padding: "2px" }}>
      SideBar
    </Box>
  );
};

export default SideBar;
